
<div class="skills">
    <div class="text-header">{{ item.skills }}</div>
    <div class="skills_images">
        <div class="images">
            {% for image in item.images %}
                <img src="{{ image }}" alt="project-img" class="skills-image">
            {% endfor %}
        </div>
    </div>
</div>
